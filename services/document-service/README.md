# Document service

Сервис документов. Генерирует PDF-счёт и PDF-чек, сохраняет файлы в storage volume и метаданные в PostgreSQL.

Сервис входит в состав RetailOps-microservices-platform и запускается как отдельный FastAPI container.
